image: node:latest

stages:
  - build
  - run

# Job's name
first-job:
  # Define stage
  stage: build
  # What to run on the job.
  tags:
    - "ci"
  script:
    - npm install
  artifacts:
    paths:
      - /home/gitlab-runner/
